export enum LoginAPI {
  // 登录
  AccountLogin = '/login',
  // 用户信息
  UserInfo = '/userinfo',
  // 菜单信息
  UserMenus = '/usermenus'
}

export enum PageNameType {
  users = 'user',
  role = 'role'
}

export enum SystemPageListUrl {
  // 用户列表
  userList = '/users/list',
  // 规则
  roleList = '/role/list'
}
